#include <string>

using namespace std;

struct InfoForDoc {
    string name;
    string short_description;
    string description;
};

struct MemberInfo {
    string name;
    string short_description;
};